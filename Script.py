# import os

# def find_file(filename):
#     for root, dirs, files in os.walk('/'):
#         if filename in files:
# #             return os.path.join(root, filename)
# #     return None

# import itertools

# start_time = time.perf_counter()
# arr_5 = map(lambda x: ([x[0], x[1]]), itertools.product(data, repeat=2))
# total_time_5 = time.perf_counter()-start_time

# print(tuple(arr_5))